<% form_for current_beverage do |f| %>
  <div>
    <label>Name:</label>
    <%= f.text_field :name %>
  </div>
  <div>
    <label>Type:</label>
    <%= f.collection_select :beverage_type_id, BeverageType.all, :id, :name %>
  </div>
  <%= submit_tag 'Save' %>
<% end %>